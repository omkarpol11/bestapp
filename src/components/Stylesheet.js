import React from 'react'
import './Style.css';

function Stylesheet(props) {
    let claaName=props.isvalue ? 'demo' : 'demo1';
      return (
        <div>
            <h1 className={`${className} demo2`}>hellloo my frends</h1>
        </div>
    )
}

export default Stylesheet

